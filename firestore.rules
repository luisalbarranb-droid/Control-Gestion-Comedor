rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Durante desarrollo, permitimos todo
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
