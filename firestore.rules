
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // --- Funciones de Ayuda (Helpers) ---
    function isAuthenticated() {
      return request.auth != null;
    }

    function getUserData(userId) {
        // La primera vez, esto puede fallar si el documento no existe.
        // Por eso, las reglas iniciales deben ser más permisivas.
        return get(/databases/$(database)/documents/users/$(userId)).data;
    }

    function isSuperAdmin() {
      let userData = getUserData(request.auth.uid);
      return isAuthenticated() && userData != null && userData.role == 'superadmin';
    }

    function isAdmin() {
      let userData = getUserData(request.auth.uid);
      return isAuthenticated() && userData != null && (userData.role == 'admin' || userData.role == 'superadmin');
    }
    
    function isOwner(userId) {
      return isAuthenticated() && request.auth.uid == userId;
    }
    
    function cannotChange(field) {
      return resource.data[field] == request.resource.data[field];
    }

    // ------------------------------------------------------------------
    // --- 1. User Data Rules (CRÍTICO - ABIERTO PARA CONFIGURACIÓN) ---
    // ------------------------------------------------------------------
    match /users/{userId} {
      // PERMISO TEMPORAL: Permitir que cualquier usuario autenticado pueda leer y escribir
      // para romper el ciclo inicial de permisos y crear el primer superadmin.
      // Una vez configurado el superadmin, esta regla debe ser más estricta.
      allow read, write: if isAuthenticated();
    }

    // ----------------------------------
    // --- 2. Configuration & Control ---
    // ----------------------------------
    match /areas/{areaId} {
      allow read: if isAuthenticated();
      allow write: if isSuperAdmin(); // Solo SuperAdmin puede gestionar áreas
    }

    match /configuration/{configId} {
      allow read: if isAuthenticated();
      allow write: if isSuperAdmin(); // Configuración global, solo SuperAdmin.
    }
    
    match /statistics/{period} {
      allow read: if isAdmin();
      allow write: if isSuperAdmin(); 
    }

    // -----------------------------------------------
    // --- 3. Task Management (CORREGIDO PARA LISTAR) ---
    // -----------------------------------------------
    match /tasks/{taskId} {
      // Lectura de un solo documento: El asignado, el creador, o un admin.
      allow get: if isAuthenticated() && (
          resource.data.assignedTo == request.auth.uid ||
          resource.data.createdBy == request.auth.uid ||
          (hasRole('admin') && getUserData(request.auth.uid).areas.hasAny([resource.data.area])) ||
          isSuperAdmin()
      );
      
      // Listado de la colección: Permitido para cualquier usuario autenticado para la configuración inicial.
      // Una vez que el Superadmin esté creado, se puede cambiar a if isAdmin().
      allow list: if isAuthenticated();
      
      // Creación: Cualquier usuario activo puede crear una tarea.
      allow create: if isAuthenticated();
      
      // Escritura (Update/Delete): Solo el creador de la tarea o un Admin/SuperAdmin.
      allow update, delete: if isAuthenticated() && (resource.data.createdBy == request.auth.uid || isAdmin());
    }

    // ------------------------------
    // --- 4. Inventory & Menus ---
    // ------------------------------
    match /inventory/{itemId} {
      allow read: if isAuthenticated();
      allow write: if isAdmin();
    }

    match /inventoryTransactions/{transactionId} {
      allow read: if isAuthenticated();
      allow write: if isAdmin();
    }
    
    match /menus/{menuId} {
      allow read: if isAuthenticated();
      allow write: if isAdmin();
    }

    // --------------------------------------
    // --- 5. Attendance & Evaluation ---
    // --------------------------------------
    
    match /attendance/{recordId} {
      allow get: if isAdmin() || isOwner(resource.data.userId);
      allow list: if isAdmin();
      allow create, update, delete: if isAdmin();
    }
    
    match /daysOff/{dayOffId} {
      allow get: if isAdmin() || isOwner(resource.data.userId);
      allow list: if isAdmin();
      allow create, update, delete: if isAdmin();
    }
    
    match /evaluationCriteria/{criterionId} {
      allow get, list: if isAdmin();
      allow write: if isSuperAdmin();
    }

    match /monthlyEvaluations/{evaluationId} {
      allow get: if isOwner(resource.data.userId) || isAdmin();
      allow list: if isAdmin();
      allow create: if isAdmin(); 
      update, delete: if isAdmin();
    }
    
    match /peer-reviews/{reviewId} {
      allow read, create: if isAuthenticated();
      allow update, delete: if isAdmin();
    }
  }
}
